let message$_123="Come on my Dear";
let beHero;
beHero=message$_123; /* Copying the value of message$_123 into the beHero*/
alert(beHero);
alert(message$_123);
let $ = 1; // declared a variable with the name "$"
let _ = 2; // and now a variable with the name "_"
alert( $ + _); // 3
const COLOR_RED = "#F00";
const COLOR_GREEN = "#0F0";
const COLOR_BLUE = "#00F";
const COLOR_ORANGE = "#FF7F00";
// ...when we need to pick a color
let color = COLOR_ORANGE;
alert(color); // #FF7F00
function test(number)
{
   while(number < 5) {
      number++;
   }
   return number;
}
alert(test(2));
var x = 0;
while(x<6) {
  x++;
}
document.write(x);